# Changelog

## [0.0.1] - 2026-06-01

### Added
- Core API interfaces (`SpidrClient`, `SpidrRequest`, `SpidrResponse`).
- DOM wrappers (`SpidrPage`, `SpidrElement`).
- Capabilities system (`SpidrCapabilities`).
- Extensibility registry (`SpidrPlugin`, `SpidrPluginRegistry`).
- Foundation smoke tests and example configuration.
