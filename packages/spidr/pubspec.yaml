name: spidr
description: A next-generation adaptive web scraping, browser automation, crawling, and data acquisition framework for Dart and Flutter.
version: 0.0.1
resolution: workspace
homepage: https://github.com/spidr-scraping/spidr
repository: https://github.com/spidr-scraping/spidr/tree/main/packages/spidr
issue_tracker: https://github.com/spidr-scraping/spidr/issues

environment:
  sdk: ^3.12.0

dependencies:
  spidr_core: ^0.0.1
  spidr_browser: ^0.0.1
  spidr_crawler: ^0.0.1
  spidr_stealth: ^0.0.1
  spidr_storage: ^0.0.1
  spidr_proxy: ^0.0.1
  spidr_ai: ^0.0.1
  meta: ^1.10.0

dev_dependencies:
  test: ^1.24.0
  lints: ^3.0.0
