# Changelog

## [0.0.1] - 2026-06-01

### Added
- Executable binary target `bin/spidr.dart`.
- Parsers for commands `scrape`, `crawl`, `browser`, `session`, `extract`, and `fingerprint`.
- CLI unit tests and execution configurations.
